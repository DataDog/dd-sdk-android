# This module looks up embedded engine view classes by name at runtime (no compile-time
# dependency on any specific embedded engine, e.g. Flutter or React Native). No keep rule is
# needed here: those view classes are shipped and protected by their own engine's consumer rules.
