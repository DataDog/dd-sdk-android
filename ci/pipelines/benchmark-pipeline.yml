stages:
  - benchmark
  - report

.snippets:
  source-secrets-macos:
    - vault login -method=aws -no-print
    - export DD_ANDROID_SECRETS_PATH_PREFIX="kv/aws/arn:aws:iam::486234852809:role/ci-dd-sdk-android/"
    - source ./ci/scripts/vault_config.sh
    - source ./ci/scripts/get-secret.sh
  setup-dd-tracer:
    - export DD_TRACER_FOLDER="$HOME/dd-java-agent"
    - mkdir -p "$DD_TRACER_FOLDER"
    - curl -Lo "$DD_TRACER_FOLDER/dd-java-agent.jar" https://dtdg.co/latest-java-tracer
    - export DD_API_KEY=$(get_secret $DD_ANDROID_SECRET__API_KEY)
    - export DD_CIVISIBILITY_AGENTLESS_ENABLED=true
  setup-dd-ci-cli:
    - npm install -g @datadog/datadog-ci
    - export DATADOG_API_KEY=$(get_secret $DD_ANDROID_SECRET__API_KEY)
  install-android-api-components:
    - echo y | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --install "emulator"
    - echo y | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --install "platform-tools"
    - echo y | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --install "$ANDROID_PLATFORM"
    - echo y | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --install "$ANDROID_BUILD_TOOLS"
    - echo y | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --install "$ANDROID_EMULATOR_IMAGE"
    - yes | ~/android_sdk/cmdline-tools/latest/bin/sdkmanager --licenses || true
    - echo "no" | ~/android_sdk/cmdline-tools/latest/bin/avdmanager --verbose create avd --force --name "$EMULATOR_NAME" --package "$ANDROID_EMULATOR_IMAGE"
  setup-macos-ami-runner:
    - !reference [ .snippets, source-secrets-macos ]
    - !reference [ .snippets, setup-dd-tracer ]
    - !reference [ .snippets, setup-dd-ci-cli ]
    - !reference [ .snippets, install-android-api-components ]

benchmark:macro:
  tags: [ "macos:sonoma", "specific:true" ]
  stage: benchmark
  timeout: 1h
  when: manual
  variables:
    ANDROID_API: "36"
    ANDROID_EMULATOR_IMAGE: "system-images;android-$ANDROID_API;google_apis;${ANDROID_ARCH}"
    ANDROID_PLATFORM: "platforms;android-$ANDROID_API"
    ANDROID_BUILD_TOOLS: "build-tools;$ANDROID_API.0.0"
  script:
    - !reference [.snippets, setup-macos-ami-runner]
    - set +e
    - exit_code=0
    - $ANDROID_HOME/emulator/emulator -avd "$EMULATOR_NAME" -grpc-use-jwt -no-snapstorage -no-audio -no-window -no-boot-anim -gpu host -verbose -qemu -machine virt &
    - GRADLE_OPTS="-Xmx3072m" ./gradlew :benchmarks:macro:connectedBenchmarkAndroidTest --stacktrace --no-daemon --no-build-cache || exit_code=$?
    - $ANDROID_HOME/platform-tools/adb emu kill
    - if [[ "$exit_code" -ne 0 ]]; then exit 1; fi
    # S3 upload and baseline download run on the macOS runner for simplicity —
    # the runner already has AWS credentials via vault login -method=aws.
    # If macOS runner costs become a concern or we add more post-processing steps,
    # consider moving these to a separate amd64 job consuming GitLab CI artifacts.
    - 'bash ci/benchmarks/s3-upload.sh || echo "Warning: S3 upload failed, continuing."'
    - 'bash ci/benchmarks/s3-download-baseline.sh || echo "Warning: baseline download failed, continuing."'
    - exit 0
  artifacts:
    when: always
    expire_in: 1 week
    paths:
      - benchmarks/macro/build/outputs/
      - candidate-dd-sdk-android.converted.json
      - baseline-dd-sdk-android.converted.json

benchmark:analyze:
  stage: report
  tags: ["arch:amd64"]
  image: registry.ddbuild.io/images/benchmarking-platform-tools-ubuntu:latest
  allow_failure: true
  needs:
    - job: benchmark:macro
      artifacts: true
  script:
    - 'bash ci/benchmarks/analyze.sh || echo "Warning: analysis failed, continuing."'
  artifacts:
    when: always
    expire_in: 1 week
    paths:
      - benchmark-comparison.md

benchmark:micro:
  tags: [ "macos:sonoma", "specific:true" ]
  stage: benchmark
  timeout: 1h
  when: manual
  variables:
    ANDROID_API: "36"
    ANDROID_EMULATOR_IMAGE: "system-images;android-$ANDROID_API;google_apis;${ANDROID_ARCH}"
    ANDROID_PLATFORM: "platforms;android-$ANDROID_API"
    ANDROID_BUILD_TOOLS: "build-tools;$ANDROID_API.0.0"
  script:
    - !reference [.snippets, setup-macos-ami-runner]
    - set +e
    - exit_code=0
    - $ANDROID_HOME/emulator/emulator -avd "$EMULATOR_NAME" -grpc-use-jwt -no-snapstorage -no-audio -no-window -no-boot-anim -gpu host -verbose -qemu -machine virt &
    - GRADLE_OPTS="-Xmx3072m" ./gradlew :benchmarks:micro:connectedReleaseAndroidTest --stacktrace --no-daemon --no-build-cache || exit_code=$?
    - $ANDROID_HOME/platform-tools/adb emu kill
    - if [[ "$exit_code" -ne 0 ]]; then exit 1; fi
    - exit 0
  artifacts:
    when: always
    expire_in: 1 week
    paths:
      - benchmarks/micro/build/outputs/
